<UserControl x:Class="Ebook_Reader.Views.BooksView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="0,0,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header Title -->
        <TextBlock Grid.Row="0" Text="My Books Collection" Style="{StaticResource DashboardTitle}" Margin="0,0,0,25"/>

        <!-- Scrollable Grid of Books -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Books}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Style="{StaticResource MaterialDesignFlatButton}" 
                                Command="{Binding DataContext.OpenBookCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                CommandParameter="{Binding}"
                                Width="170" Height="290" Margin="0,0,25,25" Padding="0" Cursor="Hand">
                            <StackPanel>
                                <Border CornerRadius="12" Height="230" Background="White" ClipToBounds="True">
                                    <Border.Effect>
                                        <StaticResource ResourceKey="SoftShadow"/>
                                    </Border.Effect>
                                    <Grid>
                                        <materialDesign:PackIcon Kind="Book" Width="50" Height="50" Foreground="{StaticResource AccentCoralBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        <Image Source="{Binding CoverImagePath}" Stretch="UniformToFill"/>
                                    </Grid>
                                </Border>
                                <TextBlock Text="{Binding Title}" FontWeight="Bold" Margin="0,10,0,2" Foreground="{StaticResource DeepTextBrush}" TextTrimming="CharacterEllipsis"/>
                                <TextBlock Text="{Binding Author}" FontSize="12" Foreground="{StaticResource MutedTextBrush}" TextTrimming="CharacterEllipsis"/>
                            </StackPanel>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
