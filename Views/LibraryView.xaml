<UserControl x:Class="Ebook_Reader.Views.LibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="7*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <!-- Left Content Area -->
        <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto">
            <Grid Margin="20,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/> <!-- Greeting -->
                    <RowDefinition Height="Auto"/> <!-- Section Title -->
                    <RowDefinition Height="350"/>  <!-- Books Section -->
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Greeting -->
                <StackPanel Grid.Row="0" Margin="0,20,0,40">
                    <TextBlock Text="Happy reading," Style="{StaticResource DashboardTitle}"/>
                    <TextBlock Text="Cami" Style="{StaticResource DashboardTitle}" Foreground="{StaticResource AccentCoralBrush}"/>
                    <TextBlock Margin="0,15,0,0" Width="350" HorizontalAlignment="Left"
                               Style="{StaticResource DashboardSubtitle}"
                               Text="Wow! you've delved deep into your reading journey. Ready to discover what's next? Get reading now!"/>

                    <Button Style="{StaticResource MaterialDesignFlatMidBgButton}" 
                            HorizontalAlignment="Left" Margin="0,25,0,0"
                            Background="{StaticResource DeepTextBrush}" Foreground="White"
                            materialDesign:ButtonAssist.CornerRadius="20" Padding="25,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Start reading"/>
                            <materialDesign:PackIcon Kind="ArrowRight" Margin="10,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </StackPanel>

                <!-- Recently Added Section Title -->
                <TextBlock Grid.Row="1" Text="Añadidos recientemente" FontSize="18" FontWeight="Bold" Margin="0,0,0,20" Foreground="{StaticResource DeepTextBrush}"/>

                <!-- Books Section -->
                <ScrollViewer Grid.Row="2" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled" 
                              Padding="0,0,0,20">
                    <ItemsControl ItemsSource="{Binding RecentBooks}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Top"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Style="{StaticResource MaterialDesignFlatButton}" 
                                        Command="{Binding DataContext.OpenBookCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"
                                        Width="160" Height="300" Margin="0,0,20,0" Padding="0" Cursor="Hand" VerticalAlignment="Top">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="220"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>

                                        <Border Grid.Row="0" CornerRadius="10" Background="White" ClipToBounds="True">
                                            <Border.Effect>
                                                <StaticResource ResourceKey="SoftShadow"/>
                                            </Border.Effect>
                                            <Grid Background="#F9F9F9">
                                                <materialDesign:PackIcon Kind="Book" Width="40" Height="40" Foreground="{StaticResource AccentCoralBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.3"/>
                                                <Image Source="{Binding CoverImagePath}" Stretch="Uniform" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                            </Grid>
                                        </Border>

                                        <TextBlock Grid.Row="1" Text="{Binding Title}" FontWeight="Bold" Margin="0,15,0,2" Foreground="{StaticResource DeepTextBrush}" TextTrimming="CharacterEllipsis" FontSize="14"/>
                                        <TextBlock Grid.Row="2" Text="{Binding Author}" FontSize="12" Foreground="{StaticResource MutedTextBrush}"/>
                                    </Grid>
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </ScrollViewer>

        <!-- Right Side Panel -->
        <StackPanel Grid.Column="1" Margin="20,0,0,0" DataContext="{Binding SelectedBook}">
            <!-- Summary Book Card -->
            <Border Background="White" CornerRadius="20" Padding="25" Margin="0,0,0,30">
                <Border.Effect>
                    <StaticResource ResourceKey="SoftShadow"/>
                </Border.Effect>
                <StackPanel>
                    <Border Height="200" Width="140" Background="{StaticResource HighlightYellowBrush}" CornerRadius="10" Margin="0,0,0,20" ClipToBounds="True">
                        <Grid>
                            <materialDesign:PackIcon Kind="BookOpenPageVariant" Width="60" Height="60" Foreground="{StaticResource AccentCoralBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.5"/>
                            <Image Source="{Binding CoverImagePath}" Stretch="UniformToFill" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Grid>
                    </Border>
                    <TextBlock Text="{Binding Title, FallbackValue='No book selected'}" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" TextAlignment="Center" Foreground="{StaticResource DeepTextBrush}" TextTrimming="CharacterEllipsis"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10">
                        <TextBlock Text="{Binding CurrentPage}" Foreground="{StaticResource AccentCoralBrush}" FontWeight="SemiBold"/>
                        <TextBlock Text=" / " Foreground="{StaticResource AccentCoralBrush}" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding TotalPages}" Foreground="{StaticResource AccentCoralBrush}" FontWeight="SemiBold"/>
                        <TextBlock Text=" pages" Foreground="{StaticResource AccentCoralBrush}" FontWeight="SemiBold"/>
                    </StackPanel>
                    <TextBlock Text="{Binding Description}" MaxHeight="80" TextWrapping="Wrap"
                               Style="{StaticResource DashboardSubtitle}" TextAlignment="Center" Margin="0,10" TextTrimming="CharacterEllipsis"/>
                    <TextBlock Text="{Binding Author, StringFormat='- {0}'}" FontStyle="Italic" HorizontalAlignment="Right" FontSize="12" Foreground="{StaticResource DeepTextBrush}"/>
                </StackPanel>
            </Border>

            <!-- Schedule Reading -->
            <Grid Margin="0,0,0,10">
                <TextBlock Text="Schedule Reading" FontWeight="Bold" Foreground="{StaticResource DeepTextBrush}"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <materialDesign:PackIcon Kind="ChevronLeft" Width="18" Foreground="{StaticResource MutedTextBrush}"/>
                    <materialDesign:PackIcon Kind="ChevronRight" Width="18" Foreground="{StaticResource MutedTextBrush}"/>
                </StackPanel>
            </Grid>
            <UniformGrid Columns="7" Margin="0,0,0,30">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="Sun" FontSize="10" Foreground="{StaticResource MutedTextBrush}"/>
                    <TextBlock Text="11" Foreground="{StaticResource DeepTextBrush}"/>
                </StackPanel>
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="Mon" FontSize="10" Foreground="{StaticResource MutedTextBrush}"/>
                    <TextBlock Text="12" Foreground="{StaticResource DeepTextBrush}"/>
                </StackPanel>
                <Border Background="{StaticResource HighlightYellowBrush}" CornerRadius="10" Padding="5">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="Tue" FontSize="10" Foreground="{StaticResource AccentCoralBrush}" FontWeight="Bold"/>
                        <TextBlock Text="13" Foreground="{StaticResource DeepTextBrush}" FontWeight="Bold"/>
                    </StackPanel>
                </Border>
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="Wed" FontSize="10" Foreground="{StaticResource MutedTextBrush}"/>
                    <TextBlock Text="14" Foreground="{StaticResource DeepTextBrush}"/>
                </StackPanel>
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="Thu" FontSize="10" Foreground="{StaticResource MutedTextBrush}"/>
                    <TextBlock Text="15" Foreground="{StaticResource DeepTextBrush}"/>
                </StackPanel>
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="Fri" FontSize="10" Foreground="{StaticResource MutedTextBrush}"/>
                    <TextBlock Text="16" Foreground="{StaticResource DeepTextBrush}"/>
                </StackPanel>
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="Sat" FontSize="10" Foreground="{StaticResource MutedTextBrush}"/>
                    <TextBlock Text="17" Foreground="{StaticResource DeepTextBrush}"/>
                </StackPanel>
            </UniformGrid>

            <!-- Reader Friends -->
            <TextBlock Text="Reader Friends" FontWeight="Bold" Margin="0,0,0,20" Foreground="{StaticResource DeepTextBrush}"/>
            <StackPanel>
                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Ellipse Width="40" Height="40">
                        <Ellipse.Fill>
                            <ImageBrush ImageSource="https://ui-avatars.com/api/?name=Roberto+Jordan&amp;background=random"/>
                        </Ellipse.Fill>
                    </Ellipse>
                    <StackPanel Grid.Column="1" Margin="15,0">
                        <TextBlock Text="Roberto Jordan" FontWeight="Bold" Foreground="{StaticResource DeepTextBrush}"/>
                        <TextBlock Text="What a delightful and magical chapter..." Style="{StaticResource DashboardSubtitle}" MaxHeight="35"/>
                    </StackPanel>
                </Grid>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Ellipse Width="40" Height="40">
                        <Ellipse.Fill>
                            <ImageBrush ImageSource="https://ui-avatars.com/api/?name=Anna+Henry&amp;background=random"/>
                        </Ellipse.Fill>
                    </Ellipse>
                    <StackPanel Grid.Column="1" Margin="15,0">
                        <TextBlock Text="Anna Henry" FontWeight="Bold" Foreground="{StaticResource DeepTextBrush}"/>
                        <TextBlock Text="I finished reading the chapter last night..." Style="{StaticResource DashboardSubtitle}" MaxHeight="35"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
