<Application x:Class="Ebook_Reader.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Ebook_Reader"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <materialDesign:BundledTheme BaseTheme="Light" PrimaryColor="DeepPurple" SecondaryColor="DeepPurple" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesign2.Defaults.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!-- Custom Colors inspired by mockup -->
            <SolidColorBrush x:Key="PrimaryBackgroundBrush" Color="#F2EEE3" />
            <SolidColorBrush x:Key="SidebarBackgroundBrush" Color="#FFFFFF" />
            <SolidColorBrush x:Key="AccentCoralBrush" Color="#E76E6E" />
            <SolidColorBrush x:Key="HighlightYellowBrush" Color="#FDF8E1" />
            <SolidColorBrush x:Key="DeepTextBrush" Color="#1A1A1A" />
            <SolidColorBrush x:Key="MutedTextBrush" Color="#8C8C8C" />
            
            <!-- Shadow Effects -->
            <DropShadowEffect x:Key="SoftShadow" BlurRadius="15" ShadowDepth="2" Direction="270" Opacity="0.1" />

            <!-- Base Styles -->
            <Style x:Key="DashboardTitle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="32" />
                <Setter Property="FontWeight" Value="Bold" />
                <Setter Property="Foreground" Value="{StaticResource DeepTextBrush}" />
                <Setter Property="FontFamily" Value="Segoe UI Semibold" />
            </Style>

            <Style x:Key="DashboardSubtitle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="14" />
                <Setter Property="Foreground" Value="{StaticResource MutedTextBrush}" />
                <Setter Property="TextWrapping" Value="Wrap" />
            </Style>

        </ResourceDictionary>
    </Application.Resources>
</Application>
